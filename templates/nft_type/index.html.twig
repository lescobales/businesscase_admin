{% extends 'base.html.twig' %}

{% block title %}Hello NftTypeController!{% endblock %}

{% block body %}
<div class="container w-75">
<div>
        <a href="{{ path('app_nft_type_add')}}"><i class="bi bi-plus-circle fs-1 text-warning"></i></i></a>
      </div>
    <table class="table">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Designation</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody>
        {% for type in types %}
          <tr>
            <th scope="row">{{ type.id }}</th>
            <td>{{ type.designation }}</td>
            <td>
                <a href="{{ path('app_nft_type_update', { 'id':type.id }) }}"><i class="bi bi-pencil"></i></a>
            </td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
</div>
{% endblock %}
