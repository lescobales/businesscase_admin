{% extends 'base.html.twig' %}

{% block title %}Update User{% endblock %}

{% block body %}
    <div class="container w-75">
        {{ form_start(form) }}
                <div>{{ form_row(form.name) }}</div>
                <div><img src="{{ form.representation.vars.value }}"></div>
        {{ form_end(form) }}
    </div>
{% endblock %}
